---
interface Props {
  slot?: string;
  format?: 'horizontal' | 'rectangle' | 'auto';
  className?: string;
}

const { slot = '7164999486301748', format = 'auto', className = '' } = Astro.props;
const CLIENT = 'ca-pub-7164999486301748';
---
<div class={`ad-slot-wrapper no-print my-6 w-full text-center ${className}`}>
  <ins 
    class="adsbygoogle block mx-auto text-center"
    style="display:block"
    data-ad-client={CLIENT}
    data-ad-slot={slot}
    data-ad-format={format}
    data-full-width-responsive="true"
  ></ins>
  <script is:inline>
    try {
      (window.adsbygoogle = window.adsbygoogle || []).push({});
    } catch (e) {}
  </script>
</div>
